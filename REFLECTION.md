# REFLECTION.md — 项目反思报告


## 一、Superpowers 技能：哪些是放大器，哪些是形式

**放大器**：`brainstorming` 逼我回答"你到底想做什么"。最初我想做"一个通用 agent 框架"，被追问"谁是用户、六个维度哪个做深"后收敛为"治理机制（护栏/HITL）为重的 coding harness"——这个收敛决定了整个项目的深度走向，也让我第一次意识到：LLM 能写代码，但"做什么"必须人来定。`writing-plans` 把设计切成"2–5 分钟、明确文件、明确验证"的 task，后期执行几乎不用回头。`test-driven-development` 是最强的纪律武器，详见第二节。

**形式大于实质**：`using-git-worktrees` 对单人项目收益有限——没有并行协作者，worktree 的隔离价值被合并成本抵消，后期我实际上主要在主分支上推进。`AGENT_LOG` 的部分模板化记录（时间戳+技能名）有"为记录而记录"的倾向；两阶段评审在部分 task 上流于"找茬"，真正有价值的评审发生在高风险机制（护栏、审批）上。

## 二、TDD 在 AI 协作下是阻碍还是放大器

**是放大器，而且是本轮最强的心智工具**。先写失败测试的本质，是把"验收标准"翻译成机器可判定的断言，从而**消除 subagent 的自由裁量**——它不能再靠"我觉得实现了"交差。典型例子：护栏的 fail-closed 行为（T11 的 nil Guard 装配），评审两轮都抓到真实漏洞，靠的就是先写"无护栏时默认拦截"的失败测试。

但 TDD 有盲区：它只能验证"我写出的断言"，验证不了"我没意识到的假设"。真实 LLM 验收一上来就发现两个 schema 缺陷——`run_test` 的 `required:null` 和 `tool_calls` 缺 `type:"function"`——而全量 mock 单测 12 个包全绿。这印证了课程 §A.4-C 的判据：**确定性测试验证"机制逻辑"，验证不了"与外部真实系统的契约合规"**。两者必须互补。

## 三、subagent 能自主运行多久？什么颗粒度最优？

单个 subagent 能可靠完成一个"2–5 分钟、路径明确、有可断言验收"的 task；一旦任务跨模块、或验收标准含糊，它就开始漂移，本次会话共发生 5 次 subagent 中断/崩溃。恢复模式很固定：查 worktree 的 git 状态 → 由协调者补齐 → 在 AGENT_LOG 记录偏离。**最优颗粒度 = 一个 subagent 一次会话可完成 + 有失败测试兜底**。再小则协调开销超过收益，再大则漂移率陡增。

## 四、SPEC/PLAN 质量如何影响实现质量

最直接的证据是冷启动验证：换了一个完全陌生的 agent（OpenCode），只给 SPEC+PLAN，它暴露了 4 处我从未意识到的规约缺陷——工具链前提未写、GOPROXY 网络现实、fence 语义的两层定义、规则命名约定。**"规约不清导致 subagent 偏离"的典型案例**：`run_shell` 工具的 JSON schema 漏了 `command` 必填字段，SPEC 只写了"提供 shell 工具"，没写"参数 schema 必须完整声明且可被真实 LLM 消费"，subagent 按最小实现产出，直到真实 LLM 无法调用才暴露。教训：**验收标准若不能写成断言，就会在实现阶段以各种形式漏回来**。

## 五、最有效的 prompt / context 策略

三条最有效：① 把 diff 先写入文件再交评审 subagent，而不是贴进对话——直接贴 diff 会污染上下文，这是踩过坑后的血泪经验；② 每个 task 的 prompt 内嵌"文件路径 + 验证步骤 + 不确定即暂停"；③ 冷启动验证的"陌生 agent"机制，是单人项目里最接近同侪评审的东西，强烈建议保留。

## 六、凭据与分发：被工程要求逼着想清楚的事

凭据部分原本只想"存个 key 就行"，是 §3.1 逼我画出威胁模型：桌面端 keyring（隐藏录入）、`.env`（明文风险）、云端容器无 keyring → 新增 `EnvProvider` 读环境变量，三级回退链。分发部分最大的教育是**"全新机器从零运行"的现实**：`proxy.golang.org` 不可达、`gcr.io` 被墙、Docker Hub 下载被截断、GHCR 包默认私有——这些全是文档里不会写、只有真跑一遍才知道的工程现实。部署从 Render 一路换到阿里云 ECS、又发现实例是 Windows Server，最终靠 Workbench CLI 打通——每一次弯路都是"假设 vs 现实"的碰撞。

## 七、如果重做，我会改变什么

① 更早做"真实 LLM 冒烟测试"——哪怕只调一次 API，也不至于把两个 schema bug 留到验收阶段；② 部署选型先确认目标 OS 与网络（避免在 Windows 实例上白费一套方案）；③ AGENT_LOG 只记"偏离与教训"，不记流水账。

## 八、对 Superpowers 方法论的批判

它假设"单人也能像成熟团队一样运转"：评审、worktree、日志、两阶段把关。这些假设在本项目里**大部分成立**——纪律是真的守住了 TDD 和"先验证再宣称完成"。但它也有三个盲区：① 假设 AI 会遵守流程，实际它漂移得靠人盯；② 对"与外部真实系统（LLM API、云平台）的契约合规"没有机制覆盖，这正是本次两个 schema bug 的温床；③ 流程本身会膨胀成形式主义。**方法论给你脚手架，但"做什么"和"做对了吗"永远要靠人**——这句话我从"听老师说过"变成了"自己撞过"。
